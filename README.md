# Card Rewards Data Pipeline

A medallion-architecture batch pipeline (bronze → silver → gold) that turns raw
card-transaction feeds into rewards-ROI marts, built with PySpark, Spark SQL
and Airflow.

Implementation lands in the first pull request.
